apiVersion: v1
kind: Service
metadata:
  labels:
    app: nacos
  name: nacos
  namespace: default
spec:
  ports:
  - name: server
    port: 8848
    protocol: TCP
    targetPort: 8848
  selector:
    app: nacos
  sessionAffinity: None
  type: LoadBalancer
